import React from 'react'
import type { Metadata } from "next";

export const metadata:Metadata={
    title:"Dashboard",
    description:"Dashboard page of AI Code Reviewer."
}

const Dashboard = () => {
  return (
    <div>Dashboard</div>
  )
}

export default Dashboard;